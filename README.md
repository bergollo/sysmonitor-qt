# qt-sysmonitor

Minimal Qt 6 Widgets application used to verify the CMake build setup.

## Build and Run

```bash
cmake -S . -B build
cmake --build build
./build/HelloWorld
```

The only application source is `src/main.cpp`.
